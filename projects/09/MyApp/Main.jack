/** Initializes a new Square Dance game and starts running it. */
class Main {
    function void main() {
        var DirtyGame game;
        let game = DirtyGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}